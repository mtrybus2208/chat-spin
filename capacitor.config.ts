import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mtrybus.threemeals',
  appName: 'Three meals',
  webDir: 'dist/apps/three-meals/browser',
};

export default config;
